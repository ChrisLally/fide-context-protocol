---
title: FCP Docs
description: Protocol documentation source owned by the FCP package.
---

This page is served by `apps/docs`, but the content lives in `packages/fide-context-protocol/docs`.

## Why this structure

- Protocol docs stay close to protocol code.
- Contributors can update code and docs in one PR.
- `apps/docs` remains a composition/rendering shell.
