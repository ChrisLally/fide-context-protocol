---
title: "Action"
description: "A discrete assertion or commitment attributable to an Agent. Often produces or records a change (e.g., a transaction, API call, signature). Can be linked to Events via Statements (causes, records, resultsIn)."
full: true
---

<div className="flex flex-col gap-4 mt-4 mb-8">
  <div className="flex items-center gap-2">
    <strong className="min-w-[150px]">Definition:</strong>
    <span className="text-sm">A discrete assertion or commitment attributable to an Agent. Often produces or records a change (e.g., a transaction, API call, signature). Can be linked to Events via Statements (causes, records, resultsIn).</span>
  </div>
  <div className="flex items-center gap-2">
    <strong className="min-w-[150px]">Not:</strong>
    <span className="text-sm">Not a broad temporal happening (Event); must be driven by an Agent.</span>
  </div>
  <div className="flex items-center gap-2">
    <strong className="min-w-[150px]">Layer:</strong>
    <a href="/docs/fcp/vocabulary#spacetime" className="text-primary hover:underline font-medium">Spacetime</a>
  </div>
  <div className="flex items-center gap-2">
    <strong className="min-w-[150px]">Hex Code:</strong>
    <code className="px-2 py-0.5 rounded-md bg-secondary text-secondary-foreground font-mono text-sm">42</code>
  </div>
  <div className="flex items-center gap-2">
    <strong className="min-w-[150px]">Standard Alignment:</strong>
    <span className="px-2 py-0.5 rounded-md bg-secondary text-secondary-foreground text-sm">schema:Action + prov:Activity</span>
    <span className="text-muted-foreground text-sm">(Close fit)</span>
  </div>
</div>
