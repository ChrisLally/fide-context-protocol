---
title: Statement Schema
description: Core statement model and constraints for FCP assertions.
---

## Scope

- Define the protocol-level statement model.
- Define required fields and canonical semantics.
- Define what is out of scope (product/runtime concerns).

## Boundary Notes

- Identifier calculation and normalization are defined in [Identifiers](/docs/fcp/identifiers).
- This page defines how identifiers are used inside statements, not how identifiers are derived.
- Entity/type authority comes from [Vocabulary](/docs/fcp/vocabulary).

## Core Model

- Subject, predicate, object requirements.
- Allowed entity/value forms.
- Canonical statement representation.

## Constraints

- Required fields and validation rules.
- Identifier linkage requirements.
- Normalization and serialization expectations.

## Interoperability

- Deterministic behavior across implementations.
- Backward compatibility notes.
- Relationship to generated vocabulary pages as the canonical type source.
